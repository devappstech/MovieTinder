import React, { PropTypes, Component } from 'react';
import {
  StyleSheet,
  Text,
  View
} from 'react-native';

export default class ProfilView extends Component {
  render() {
    return (
      <View flex={1}>
        <Text>Profil</Text>
      </View>
    );
  }
}
